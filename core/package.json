{
  "name": "@mew/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MEW Core — local-first profile server (Bun + SQLite). Reuses app/src domain + state; clients connect over a localhost API. See docs/spikes/mew-core-runtime.md.",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.6.0"
  }
}
