{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least privilege: the SPA needs only the core window APIs. FS (auto-backup) and oauth arrive with their phases, each with its own narrow grant.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
